- Modified /usr/lib/systemd/system/tomcat10.service:
  Line 12:
  From:
  Environment=CATALINA_OPTS=
  To:
  Environment=CATALINA_OPTS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005

- journalctl -u tomcat10.service --since "3 min ago"

- . /home/dev/Documents/commander/dev/deploy.sh && sudo systemctl restart tomcat10

- ignore unrecognized/unsupported arguments (as opposed to returning 400)

- validation/exception order:

  Problem: The legal domain (non-null, range) of a parameter may change
  depending on the client's authentication, the client's authorities, and the
  internal state of the application (which can be indirectly leaked by returning
  status codes that reveal hints about the state).

  1.  Validate argument types
      [MethodArgumentTypeMismatchException 400]

  2.  Validate arguments are within coarse-grained legal domains (don't consider authentication, authorization, or internal state)
      [IllegalArgumentException 400]

  3.  Validate client is authenticated
      [NotAuthenticatedException 401]

  4.  Validate client is authorized
      [NotAuthorizedException 403]

  5.  Validate arguments are within fine-grained legal domains (consider authentication, authorization, and internal state)
      [IllegalArgumentException 400]

  6.  Execute operation
      [NotFoundException 404, ConflictException 409]

TODO

- custom HTTP responses for all errors (404, 405, etc)
